'use strict';

// a function that prints your name and a nice message
//// printMessage(name) > returns a string
//// output a string
// our app will console log the returned string
// it would be nice to have your name as a command line argument

